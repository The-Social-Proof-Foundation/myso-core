# Example - Use a MEV bot to Clear Expired Orderbook Orders

## Overview

This example shows how to use a MEV bot to clear expired Orderbook orders. The bot scans Orderbook pools for expired orders and cancels them. The example bot uses the MySo TS SDK to interact with the MySo Full node and retrieve on chain Orderbook data.

## How to run
```bash
pnpm start
```
