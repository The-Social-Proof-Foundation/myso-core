---
title: Module `social_contracts::mydata_pool` (removed)
---

Query marketplace types and entrypoints live in [`social_contracts::mydata`](./mydata.md). `bootstrap_init` creates pool/anchor/vault objects together with config and registry; use `create_mydata_pool_admin_cap` for the pool admin cap.
