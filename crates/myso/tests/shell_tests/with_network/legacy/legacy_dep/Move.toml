# Legacy package with only framework dependency
[package]
name = "LegacyDep"
edition = "2024"
implicit-dependencies = false

[dependencies]
MySo = { local = "../myso-framework" }

[addresses]
legacy_dep = "0x0"
