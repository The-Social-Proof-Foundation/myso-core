[package]
name = "j"
edition = "2024"
implicit-dependencies = false

[dependencies]
# this is relative to the temp directory where this whole tree shaking dir is copied to
myso = { local = "../../system-packages/myso-framework", override = true }
