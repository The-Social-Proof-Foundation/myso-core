---
apiVersion: v1
kind: Service
metadata:
  name: myso-node
  namespace: rpc-mainnet
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: myso-node
  type: ClusterIP
---
allowVolumeExpansion: true
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: pd-ssd
parameters:
  type: pd-ssd
provisioner: kubernetes.io/gce-pd
reclaimPolicy: Delete
volumeBindingMode: Immediate
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: myso-node
  name: myso-node
  namespace: rpc-mainnet
spec:
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: myso-node
  serviceName: myso-node
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "9184"
        prometheus.io/scrape: "true"
      labels:
        app: myso-node
    spec:
      containers:
      - command:
        - /opt/myso/bin/myso-node
        - --config-path
        - /opt/myso/config/myso-node.yaml
        env:
        - name: RUST_BACKTRACE
          value: "1"
        - name: RUST_LOG
          value: info,myso_core=debug,myso_network=debug,consensus=debug,jsonrpsee=error
        image: x
        imagePullPolicy: Always
        name: myso-node
        ports:
        - containerPort: 9000
          protocol: TCP
        resources:
          requests:
            cpu: "32"
            memory: 128G
        volumeMounts:
        - mountPath: /opt/myso/db
          name: ssd-storage
        - mountPath: /opt/myso/config/myso-node.yaml
          name: myso-config-volume
          subPath: myso-node.yaml
        - mountPath: /opt/myso/config/genesis.blob
          name: myso-genesis-volume
          subPath: genesis.blob
      restartPolicy: Always
      terminationGracePeriodSeconds: 180
      volumes:
      - configMap:
          defaultMode: 420
          name: myso-config
        name: myso-config-volume
      - configMap:
          defaultMode: 420
          name: myso-genesis
        name: myso-genesis-volume
  volumeClaimTemplates:
  - apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: ssd-storage
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 5000Gi
      storageClassName: pd-ssd
      volumeMode: Filesystem
